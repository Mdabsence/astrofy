---
const { items } = Astro.props;
---

<div class="carousel-container">
  <h2 class="carousel-header">Langages Pratiqués</h2>
  <p class="carousel-subheader">Les différents langages que j'ai pu rencontrer et pratiquer lors de ma formation.</p>
  

<div class="card-container">
  {items.map((item, index) => (
    <div class="card" key={index}>
      <div class="card-inner">
        <div class="card-front">
          <div class="card-front-content">
            <img src={item.img} alt={item.title} class="card-icon" />
            <h3 class="card-title">{item.title}</h3>
          </div>
        </div>
        <div class="card-back">
          <p class="card-desc">{item.desc}</p>
        </div>
      </div>
    </div>
  ))}
</div>

<style>
    .carousel-header {
      font-size: 24px;
      color: #00274D;
      margin-bottom: 5px;
  }

  .carousel-subheader {
      font-size: 16px;
      margin-bottom: 20px;
      color: #555;
  }

  .carousel {
      display: flex;
      overflow: hidden;
      width: 100%;
      transition: transform 0.5s ease-in-out;
  }

  .card-container {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* Trois colonnes de taille égale */
    gap: 20px; /* Espace entre les cartes */
    padding: 20px;
    max-width: 100%;
  }

  .card {
    width: 100%;
    height: 200px; /* Hauteur fixe pour les cartes */
    perspective: 1000px; /* Permet l'effet 3D */
  }

  .card-inner {
      width: 100%;
      height: 100%;
      transition: transform 0.6s;
      transform-style: preserve-3d;
      position: relative;
  }

  .card:hover .card-inner {
      transform: rotateY(180deg); /* Retourne la carte */
  }

  .card-front,
  .card-back {
      position: absolute;
      width: 100%;
      height: 100%;
      backface-visibility: hidden; /* Masque l'arrière lors de la rotation */
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 10px;
  }

  .card-front {
      background-color: white; /* Carte blanche */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Ombre portée */
  }

  .card-back {
      background-color: #00274D;
      color: white;
      transform: rotateY(180deg); /* Positionne le dos derrière la face avant */
  }

  .card-front-content {
      display: flex;
      flex-direction: column; /* Aligne verticalement l'icône et le titre */
      justify-content: center;
      align-items: center;
  }

  .card-icon {
      width: 60px;
      height: 60px;
      margin-bottom: 10px; /* Espace entre l'icône et le titre */
  }

  .card-title {
      font-size: 18px;
      font-weight: bold;
      color: #333;
  }

  .card-desc {
      font-size: 14px;
      color: #ddd;
  }
</style>